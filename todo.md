add custom install w.

pip install "textdescriptives[transformers]"
# spacy[tranformers]


This might require you to install rust (for tokenizers package):

curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
